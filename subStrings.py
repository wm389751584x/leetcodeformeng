""" Print all substrings of a given string

    String input = "abcd"
    First print length of 4, b/c the original string is a sub string
    second, print length of 3, then length of 2, length of 1."""
